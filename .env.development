# 開發用。npm run dev 時 Next 會自動讀這份（正式建置不會讀）。

# 資料 API 的 host
NEXT_PUBLIC_API_BASE=https://data.ltn.com.tw/brand/api

# 站台環境。不是 production 的話，robots.txt 會整站禁止索引（見 app/robots.ts）
NEXT_PUBLIC_SITE_ENV=development
